# 새벌암 (Saeburam) — Claude 세션 가이드

## 보고 원칙 (자동)
- 모든 작업 보고는 **별도 요청 없이 자동**으로, **표준 형식 + 보고 시각(KST)** 으로 한다.
- 시각은 추측 금지 → 매 턴 주입되는 `[현재 시각 — 보고 타임스탬프용]` 값 사용 (훅: `.claude/settings.json`).
- 형식·예시: `docs/STANDARD-REPORT-DIRECTIVE.md` 참조.
